define([
    'uiComponent'
], function (uiComponent) {
    'use strict';

    return uiComponent.extend({
        defaults: {
            template:  'SmartOSC_Blog/post/form/area'
        }
    })
});
